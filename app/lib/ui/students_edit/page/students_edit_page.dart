import 'package:flutter/material.dart';

class StudentsEditPage extends StatelessWidget {
  const StudentsEditPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('StudentsEditPage - TODO: implementar tela'),
      ),
    );
  }
}
