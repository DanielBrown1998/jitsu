import 'package:flutter/material.dart';

class StudentsNewPage extends StatelessWidget {
  const StudentsNewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('StudentsNewPage - TODO: implementar tela'),
      ),
    );
  }
}
