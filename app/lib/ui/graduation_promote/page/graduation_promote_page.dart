import 'package:flutter/material.dart';

class GraduationPromotePage extends StatelessWidget {
  const GraduationPromotePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('GraduationPromotePage - TODO: implementar tela'),
      ),
    );
  }
}
