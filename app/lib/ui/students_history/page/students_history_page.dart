import 'package:flutter/material.dart';

class StudentsHistoryPage extends StatelessWidget {
  const StudentsHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('StudentsHistoryPage - TODO: implementar tela'),
      ),
    );
  }
}
