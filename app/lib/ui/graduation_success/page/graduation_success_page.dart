import 'package:flutter/material.dart';

class GraduationSuccessPage extends StatelessWidget {
  const GraduationSuccessPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('GraduationSuccessPage - TODO: implementar tela'),
      ),
    );
  }
}
